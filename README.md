# core_identity_score
Predict a protein structure's backbone accuracy (average LDDT) by compare its core / surface residues to the core predicted using ESM2 embeddings

conda install -c conda-forge biopython
conda install -c conda-forge pdbfixer
